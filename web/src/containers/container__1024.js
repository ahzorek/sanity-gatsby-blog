import React from 'react';

const __1024 = {
    position: 'relative',
    width: '100%',
    maxWidth: '1024px',
    margin: '0 auto',
    padding: '0 1rem'
}

const Container = ({children}) => <div style={__1024}>{children}</div>

export default Container;
