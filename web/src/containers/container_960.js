import React from 'react';

const __960 = {
    width: '100%',
    maxWidth: '960px',
    margin: '0 auto',
    padding: '0 1rem'
}

const Container = ({children}) => <div style={__960}>{children}</div>

export default Container;
